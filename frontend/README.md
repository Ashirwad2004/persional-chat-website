this is the persional chat applicationn.
for frontend :- cd frontend
            npm install 
            npm run dev 

for backend :- cd backend
            pip install -r requirements.txt 
            uvicorn main:app --reload

for datbase :- cd datbase
                npm run migrate-all   
                